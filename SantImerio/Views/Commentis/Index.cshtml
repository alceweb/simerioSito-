@model IEnumerable<SantImerio.Models.Commenti>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
       <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th>
            Evento
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Commento)
        </th>
        <th>
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            <img src="@Url.Content("~/Content/Immagini/Utenti/" + item.UId + ".jpg")" class="img-usr" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @item.Titolo.Titolo
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Commento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Commento_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Commento_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Commento_Id })
        </td>
    </tr>
}

</table>
